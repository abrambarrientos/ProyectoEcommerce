---
const { iconClass, Texto, href } = Astro.props;
---

<a
class="w-auto h-20 bg-transparent flex flex-col items-center justify-center  transition-all hover:scale-105 rounded-md hover:border-[1px]  hover:border-[#FC8019] group" 
href={href}>
    <i class={`${iconClass} text-gray-400 group-hover:text-[#FC8019]`}></i>
    <span class="text-[10px] md:text-[15px] text-gray-400 group-hover:text-[#FC8019]">{Texto}</span>
</a>